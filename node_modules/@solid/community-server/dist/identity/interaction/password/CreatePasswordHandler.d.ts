import type { JsonRepresentation } from '../InteractionUtil';
import { JsonInteractionHandler } from '../JsonInteractionHandler';
import type { JsonInteractionHandlerInput } from '../JsonInteractionHandler';
import type { JsonView } from '../JsonView';
import type { PasswordIdRoute } from './util/PasswordIdRoute';
import type { PasswordStore } from './util/PasswordStore';
type OutType = {
    resource: string;
};
/**
 * Handles the creation of email/password login combinations for an account.
 */
export declare class CreatePasswordHandler extends JsonInteractionHandler<OutType> implements JsonView {
    protected readonly logger: import("../../..").Logger;
    private readonly passwordStore;
    private readonly passwordRoute;
    constructor(passwordStore: PasswordStore, passwordRoute: PasswordIdRoute);
    getView({ accountId }: JsonInteractionHandlerInput): Promise<JsonRepresentation>;
    handle({ accountId, json }: JsonInteractionHandlerInput): Promise<JsonRepresentation<OutType>>;
}
export {};
