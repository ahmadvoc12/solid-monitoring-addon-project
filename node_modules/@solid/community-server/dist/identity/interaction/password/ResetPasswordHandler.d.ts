import type { EmptyObject } from '../../../util/map/MapUtil';
import type { JsonRepresentation } from '../InteractionUtil';
import type { JsonInteractionHandlerInput } from '../JsonInteractionHandler';
import { JsonInteractionHandler } from '../JsonInteractionHandler';
import type { JsonView } from '../JsonView';
import type { ForgotPasswordStore } from './util/ForgotPasswordStore';
import type { PasswordStore } from './util/PasswordStore';
/**
 * Resets a password if a valid `recordId` is provided,
 * which should have been generated by a different handler.
 */
export declare class ResetPasswordHandler extends JsonInteractionHandler<EmptyObject> implements JsonView {
    protected readonly logger: import("../../..").Logger;
    private readonly passwordStore;
    private readonly forgotPasswordStore;
    constructor(passwordStore: PasswordStore, forgotPasswordStore: ForgotPasswordStore);
    getView(): Promise<JsonRepresentation>;
    handle({ json }: JsonInteractionHandlerInput): Promise<JsonRepresentation<EmptyObject>>;
    /**
     * Resets the password for the account associated with the given recordId.
     */
    private resetPassword;
}
