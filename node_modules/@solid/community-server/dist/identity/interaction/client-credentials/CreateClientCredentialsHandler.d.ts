import type { JsonRepresentation } from '../InteractionUtil';
import { JsonInteractionHandler } from '../JsonInteractionHandler';
import type { JsonInteractionHandlerInput } from '../JsonInteractionHandler';
import type { JsonView } from '../JsonView';
import type { WebIdStore } from '../webid/util/WebIdStore';
import type { ClientCredentialsIdRoute } from './util/ClientCredentialsIdRoute';
import type { ClientCredentialsStore } from './util/ClientCredentialsStore';
type OutType = {
    id: string;
    secret: string;
    resource: string;
};
/**
 * Handles the creation of client credential tokens.
 */
export declare class CreateClientCredentialsHandler extends JsonInteractionHandler<OutType> implements JsonView {
    protected readonly logger: import("../../..").Logger;
    private readonly webIdStore;
    private readonly clientCredentialsStore;
    private readonly clientCredentialsRoute;
    constructor(webIdStore: WebIdStore, clientCredentialsStore: ClientCredentialsStore, clientCredentialsRoute: ClientCredentialsIdRoute);
    getView({ accountId }: JsonInteractionHandlerInput): Promise<JsonRepresentation>;
    handle({ accountId, json }: JsonInteractionHandlerInput): Promise<JsonRepresentation<OutType>>;
}
export {};
