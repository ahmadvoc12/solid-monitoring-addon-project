import type { ResponseDescription } from '../../http/output/response/ResponseDescription';
import type { OperationHttpHandlerInput } from '../OperationHttpHandler';
import { OperationHttpHandler } from '../OperationHttpHandler';
import type { NotificationChannelStorage } from './NotificationChannelStorage';
/**
 * Allows clients to unsubscribe from notification channels.
 * Should be wrapped in a route handler that only allows `DELETE` operations.
 */
export declare class NotificationUnsubscriber extends OperationHttpHandler {
    protected readonly logger: import("../..").Logger;
    private readonly storage;
    constructor(storage: NotificationChannelStorage);
    handle({ operation }: OperationHttpHandlerInput): Promise<ResponseDescription>;
}
