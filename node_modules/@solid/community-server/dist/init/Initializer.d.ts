import { AsyncHandler } from '../util/handlers/AsyncHandler';
/**
 * Initializer is used to indicate an AsyncHandler that performs initialization logic.
 */
export declare abstract class Initializer extends AsyncHandler {
}
